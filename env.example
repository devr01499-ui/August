# Environment Variables for AdmirerX Contact Backend

# Server Configuration
PORT=3001

# Gmail SMTP Configuration
GMAIL_USER=devr01499@gmail.com
GMAIL_APP_PASSWORD=dtgx rton ntaq gwqf
NOTIFICATION_EMAIL=devr01499@gmail.com

# Google Sheets Configuration
# The google-credentials.json file should be placed in the root directory
# Make sure the service account has access to your Google Sheet

# Instructions:
# 1. Copy this file to .env
# 2. Replace GMAIL_USER with your Gmail address
# 3. Replace GMAIL_APP_PASSWORD with your Gmail App Password (already set)
# 4. Replace NOTIFICATION_EMAIL with the email where you want to receive notifications
# 5. Place your Google service account JSON file as google-credentials.json
# 6. Make sure the service account has Editor access to your Google Sheet
